# Experiments

This directory will contain Learning Units.

No LLM mechanism experiment should start until Milestone 0 is accepted and Milestone 1 is planned.

Planned first unit:

- `001-corpus-and-tokenizer/` — corpus preparation, tokenization, bigram baseline, generation, and evaluation.

Use `templates/` when creating a new Learning Unit.
